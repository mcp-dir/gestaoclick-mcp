# Ferramentas

GestãoClick expõe 127 ferramentas.

### 1. `gestaoclick_list_accounts`
**Input**: `account` (opcional)

Lista as conexões (contas) GestãoClick vinculadas a este install — id, label.

### 2. `gestaoclick_atributos_cadastros_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras de cadastros: Cadastrar (POST /api/atributos_cadastros).

### 3. `gestaoclick_atributos_cadastros_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de cadastros: Deletar (DELETE /api/atributos_cadastros/{id}).

### 4. `gestaoclick_atributos_cadastros_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras de cadastros: Visualizar (GET /api/atributos_cadastros/{id}).

### 5. `gestaoclick_atributos_cadastros_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras de cadastros: Listar (GET /api/atributos_cadastros).

### 6. `gestaoclick_atributos_cadastros_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de cadastros: Editar (PUT /api/atributos_cadastros/{id}).

### 7. `gestaoclick_atributos_financeiros_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras financeiros: Cadastrar (POST /api/atributos_financeiros).

### 8. `gestaoclick_atributos_financeiros_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras financeiros: Deletar (DELETE /api/atributos_financeiros/{id}).

### 9. `gestaoclick_atributos_financeiros_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras financeiros: Visualizar (GET /api/atributos_financeiros/{id}).

### 10. `gestaoclick_atributos_financeiros_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras financeiros: Listar (GET /api/atributos_financeiros).

### 11. `gestaoclick_atributos_financeiros_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras financeiros: Editar (PUT /api/atributos_financeiros/{id}).

### 12. `gestaoclick_atributos_orcamentos_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras de orçamentos: Cadastrar (POST /api/atributos_orcamentos).

### 13. `gestaoclick_atributos_orcamentos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de orçamentos: Deletar (DELETE /api/atributos_orcamentos/{id}).

### 14. `gestaoclick_atributos_orcamentos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras de orçamentos: Visualizar (GET /api/atributos_orcamentos/{id}).

### 15. `gestaoclick_atributos_orcamentos_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras de orçamentos: Listar (GET /api/atributos_orcamentos).

### 16. `gestaoclick_atributos_orcamentos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de orçamentos: Editar (PUT /api/atributos_orcamentos/{id}).

### 17. `gestaoclick_atributos_ordens_servicos_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras ordens serviço: Cadastrar (POST /api/atributos_ordens_servicos).

### 18. `gestaoclick_atributos_ordens_servicos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras ordens serviço: Deletar (DELETE /api/atributos_ordens_servicos/{id}).

### 19. `gestaoclick_atributos_ordens_servicos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras ordens serviço: Visualizar (GET /api/atributos_ordens_servicos/{id}).

### 20. `gestaoclick_atributos_ordens_servicos_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras ordens serviço: Listar (GET /api/atributos_ordens_servicos).

### 21. `gestaoclick_atributos_ordens_servicos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras ordens serviço: Editar (PUT /api/atributos_ordens_servicos/{id}).

### 22. `gestaoclick_atributos_produtos_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras de produtos: Cadastrar (POST /api/atributos_produtos).

### 23. `gestaoclick_atributos_produtos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de produtos: Deletar (DELETE /api/atributos_produtos/{id}).

### 24. `gestaoclick_atributos_produtos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras de produtos: Visualizar (GET /api/atributos_produtos/{id}).

### 25. `gestaoclick_atributos_produtos_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras de produtos: Listar (GET /api/atributos_produtos).

### 26. `gestaoclick_atributos_produtos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras de produtos: Editar (PUT /api/atributos_produtos/{id}).

### 27. `gestaoclick_atributos_vendas_create`
**Input**: `account` (opcional), `body` (opcional)

Campos extras vendas: Cadastrar (POST /api/atributos_vendas).

### 28. `gestaoclick_atributos_vendas_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras vendas: Deletar (DELETE /api/atributos_vendas/{id}).

### 29. `gestaoclick_atributos_vendas_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Campos extras vendas: Visualizar (GET /api/atributos_vendas/{id}).

### 30. `gestaoclick_atributos_vendas_list`
**Input**: `account` (opcional), `query` (opcional)

Campos extras vendas: Listar (GET /api/atributos_vendas).

### 31. `gestaoclick_atributos_vendas_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Campos extras vendas: Editar (PUT /api/atributos_vendas/{id}).

### 32. `gestaoclick_centros_custos_list`
**Input**: `account` (opcional), `query` (opcional)

Centros de custos: Listar (GET /api/centros_custos).

### 33. `gestaoclick_cidades_list`
**Input**: `account` (opcional), `query` (opcional)

Cidades: Listar (GET /api/cidades).

### 34. `gestaoclick_clientes_create`
**Input**: `account` (opcional), `body` (opcional)

Clientes: Cadastrar (POST /api/clientes).

### 35. `gestaoclick_clientes_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Clientes: Deletar (DELETE /api/clientes/{id}).

### 36. `gestaoclick_clientes_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Clientes: Visualizar (GET /api/clientes/{id}).

### 37. `gestaoclick_clientes_list`
**Input**: `account` (opcional), `query` (opcional)

Clientes: Listar (GET /api/clientes).

### 38. `gestaoclick_clientes_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Clientes: Editar (PUT /api/clientes/{id}).

### 39. `gestaoclick_compras_create`
**Input**: `account` (opcional), `body` (opcional)

Compras: Cadastrar (POST /api/compras).

### 40. `gestaoclick_compras_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Compras: Deletar (DELETE /api/compras/{id}).

### 41. `gestaoclick_compras_gerar_parcelas_create`
**Input**: `account` (opcional), `body` (opcional)

Compras: Gerar parcelas (POST /api/compras/gerar_parcelas).

### 42. `gestaoclick_compras_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Compras: Visualizar (GET /api/compras/{id}).

### 43. `gestaoclick_compras_list`
**Input**: `account` (opcional), `query` (opcional)

Compras: Listar (GET /api/compras).

### 44. `gestaoclick_compras_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Compras: Editar (PUT /api/compras/{id}).

### 45. `gestaoclick_contas_bancarias_list`
**Input**: `account` (opcional), `query` (opcional)

Contas bancárias: Listar (GET /api/contas_bancarias).

### 46. `gestaoclick_estados_list`
**Input**: `account` (opcional), `query` (opcional)

Estados: Listar (GET /api/estados).

### 47. `gestaoclick_formas_pagamentos_list`
**Input**: `account` (opcional), `query` (opcional)

Formas pagamentos: Listar (GET /api/formas_pagamentos).

### 48. `gestaoclick_fornecedores_create`
**Input**: `account` (opcional), `body` (opcional)

Fornecedores: Cadastrar (POST /api/fornecedores).

### 49. `gestaoclick_fornecedores_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Fornecedores: Deletar (DELETE /api/fornecedores/{id}).

### 50. `gestaoclick_fornecedores_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Fornecedores: Visualizar (GET /api/fornecedores/{id}).

### 51. `gestaoclick_fornecedores_list`
**Input**: `account` (opcional), `query` (opcional)

Fornecedores: Listar (GET /api/fornecedores).

### 52. `gestaoclick_fornecedores_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Fornecedores: Editar (PUT /api/fornecedores/{id}).

### 53. `gestaoclick_funcionarios_list`
**Input**: `account` (opcional), `query` (opcional)

Funcionários: Listar (GET /api/funcionarios).

### 54. `gestaoclick_grupos_produtos_list`
**Input**: `account` (opcional), `query` (opcional)

Grupos de produtos: Listar (GET /api/grupos_produtos).

### 55. `gestaoclick_lojas_list`
**Input**: `account` (opcional), `query` (opcional)

Lojas: Listar (GET /api/lojas).

### 56. `gestaoclick_notas_fiscais_consumidores_cancelar_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Consumidores: Cancelar (POST /api/notas_fiscais_consumidores/cancelar/{id}).

### 57. `gestaoclick_notas_fiscais_consumidores_create`
**Input**: `account` (opcional), `body` (opcional)

Notas Fiscais de Consumidores: Cadastrar (POST /api/notas_fiscais_consumidores).

### 58. `gestaoclick_notas_fiscais_consumidores_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Consumidores: Deletar (DELETE /api/notas_fiscais_consumidores/{id}).

### 59. `gestaoclick_notas_fiscais_consumidores_emitir_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Consumidores: Emitir (POST /api/notas_fiscais_consumidores/emitir/{id}).

### 60. `gestaoclick_notas_fiscais_consumidores_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Notas Fiscais de Consumidores: Visualizar (GET /api/notas_fiscais_consumidores/{id}).

### 61. `gestaoclick_notas_fiscais_consumidores_list`
**Input**: `account` (opcional), `query` (opcional)

Notas Fiscais de Consumidores: Listar (GET /api/notas_fiscais_consumidores).

### 62. `gestaoclick_notas_fiscais_consumidores_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Consumidores: Editar (PUT /api/notas_fiscais_consumidores/{id}).

### 63. `gestaoclick_notas_fiscais_produtos_cancelar_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Produtos: Cancelar (POST /api/notas_fiscais_produtos/cancelar/{id}).

### 64. `gestaoclick_notas_fiscais_produtos_create`
**Input**: `account` (opcional), `body` (opcional)

Notas Fiscais de Produtos: Cadastrar (POST /api/notas_fiscais_produtos).

### 65. `gestaoclick_notas_fiscais_produtos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Produtos: Deletar (DELETE /api/notas_fiscais_produtos/{id}).

### 66. `gestaoclick_notas_fiscais_produtos_emitir_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Produtos: Emitir (POST /api/notas_fiscais_produtos/emitir/{id}).

### 67. `gestaoclick_notas_fiscais_produtos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Notas Fiscais de Produtos: Visualizar (GET /api/notas_fiscais_produtos/{id}).

### 68. `gestaoclick_notas_fiscais_produtos_list`
**Input**: `account` (opcional), `query` (opcional)

Notas Fiscais de Produtos: Listar (GET /api/notas_fiscais_produtos).

### 69. `gestaoclick_notas_fiscais_produtos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Produtos: Editar (PUT /api/notas_fiscais_produtos/{id}).

### 70. `gestaoclick_notas_fiscais_servicos_cancelar_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Serviços: Cancelar (POST /api/notas_fiscais_servicos/cancelar/{id}).

### 71. `gestaoclick_notas_fiscais_servicos_create`
**Input**: `account` (opcional), `body` (opcional)

Notas Fiscais de Serviços: Cadastrar (POST /api/notas_fiscais_servicos).

### 72. `gestaoclick_notas_fiscais_servicos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Serviços: Deletar (DELETE /api/notas_fiscais_servicos/{id}).

### 73. `gestaoclick_notas_fiscais_servicos_emitir_create`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Serviços: Emitir (POST /api/notas_fiscais_servicos/emitir/{id}).

### 74. `gestaoclick_notas_fiscais_servicos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Notas Fiscais de Serviços: Visualizar (GET /api/notas_fiscais_servicos/{id}).

### 75. `gestaoclick_notas_fiscais_servicos_list`
**Input**: `account` (opcional), `query` (opcional)

Notas Fiscais de Serviços: Listar (GET /api/notas_fiscais_servicos).

### 76. `gestaoclick_notas_fiscais_servicos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Notas Fiscais de Serviços: Editar (PUT /api/notas_fiscais_servicos/{id}).

### 77. `gestaoclick_orcamentos_create`
**Input**: `account` (opcional), `body` (opcional)

Orçamentos: Cadastrar (POST /api/orcamentos).

### 78. `gestaoclick_orcamentos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Orçamentos: Deletar (DELETE /api/orcamentos/{id}).

### 79. `gestaoclick_orcamentos_gerar_parcelas_create`
**Input**: `account` (opcional), `body` (opcional)

Orçamentos: Gerar parcelas (POST /api/orcamentos/gerar_parcelas).

### 80. `gestaoclick_orcamentos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Orçamentos: Visualizar (GET /api/orcamentos/{id}).

### 81. `gestaoclick_orcamentos_list`
**Input**: `account` (opcional), `query` (opcional)

Orçamentos: Listar (GET /api/orcamentos).

### 82. `gestaoclick_orcamentos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Orçamentos: Editar (PUT /api/orcamentos/{id}).

### 83. `gestaoclick_ordens_servicos_create`
**Input**: `account` (opcional), `body` (opcional)

Ordens de serviços: Cadastrar (POST /api/ordens_servicos).

### 84. `gestaoclick_ordens_servicos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Ordens de serviços: Deletar (DELETE /api/ordens_servicos/{id}).

### 85. `gestaoclick_ordens_servicos_gerar_parcelas_create`
**Input**: `account` (opcional), `body` (opcional)

Ordens de serviços: Gerar parcelas (POST /api/ordens_servicos/gerar_parcelas).

### 86. `gestaoclick_ordens_servicos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Ordens de serviços: Visualizar (GET /api/ordens_servicos/{id}).

### 87. `gestaoclick_ordens_servicos_list`
**Input**: `account` (opcional), `query` (opcional)

Ordens de serviços: Listar (GET /api/ordens_servicos).

### 88. `gestaoclick_ordens_servicos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Ordens de serviços: Editar (PUT /api/ordens_servicos/{id}).

### 89. `gestaoclick_pagamentos_create`
**Input**: `account` (opcional), `body` (opcional)

Pagamentos: Cadastrar (POST /api/pagamentos).

### 90. `gestaoclick_pagamentos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Pagamentos: Deletar (DELETE /api/pagamentos/{id}).

### 91. `gestaoclick_pagamentos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Pagamentos: Visualizar (GET /api/pagamentos/{id}).

### 92. `gestaoclick_pagamentos_list`
**Input**: `account` (opcional), `query` (opcional)

Pagamentos: Listar (GET /api/pagamentos).

### 93. `gestaoclick_pagamentos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Pagamentos: Editar (PUT /api/pagamentos/{id}).

### 94. `gestaoclick_planos_contas_list`
**Input**: `account` (opcional), `query` (opcional)

Planos de contas: Listar (GET /api/planos_contas).

### 95. `gestaoclick_produtos_create`
**Input**: `account` (opcional), `body` (opcional)

Produtos: Cadastrar (POST /api/produtos).

### 96. `gestaoclick_produtos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Produtos: Deletar (DELETE /api/produtos/{id}).

### 97. `gestaoclick_produtos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Produtos: Visualizar (GET /api/produtos/{id}).

### 98. `gestaoclick_produtos_list`
**Input**: `account` (opcional), `query` (opcional)

Produtos: Listar (GET /api/produtos).

### 99. `gestaoclick_produtos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Produtos: Editar (PUT /api/produtos/{id}).

### 100. `gestaoclick_recebimentos_create`
**Input**: `account` (opcional), `body` (opcional)

Recebimentos: Cadastrar (POST /api/recebimentos).

### 101. `gestaoclick_recebimentos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Recebimentos: Deletar (DELETE /api/recebimentos/{id}).

### 102. `gestaoclick_recebimentos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Recebimentos: Visualizar (GET /api/recebimentos/{id}).

### 103. `gestaoclick_recebimentos_list`
**Input**: `account` (opcional), `query` (opcional)

Recebimentos: Listar (GET /api/recebimentos).

### 104. `gestaoclick_recebimentos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Recebimentos: Editar (PUT /api/recebimentos/{id}).

### 105. `gestaoclick_servicos_create`
**Input**: `account` (opcional), `body` (opcional)

Serviços: Cadastrar (POST /api/servicos).

### 106. `gestaoclick_servicos_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Serviços: Deletar (DELETE /api/servicos/{id}).

### 107. `gestaoclick_servicos_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Serviços: Visualizar (GET /api/servicos/{id}).

### 108. `gestaoclick_servicos_list`
**Input**: `account` (opcional), `query` (opcional)

Serviços: Listar (GET /api/servicos).

### 109. `gestaoclick_servicos_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Serviços: Editar (PUT /api/servicos/{id}).

### 110. `gestaoclick_situacoes_compras_list`
**Input**: `account` (opcional), `query` (opcional)

Situações de compras: Listar (GET /api/situacoes_compras).

### 111. `gestaoclick_situacoes_orcamentos_list`
**Input**: `account` (opcional), `query` (opcional)

Situações de orçamentos: Listar (GET /api/situacoes_orcamentos).

### 112. `gestaoclick_situacoes_ordens_servicos_list`
**Input**: `account` (opcional), `query` (opcional)

Situações de OS: Listar (GET /api/situacoes_ordens_servicos).

### 113. `gestaoclick_situacoes_vendas_list`
**Input**: `account` (opcional), `query` (opcional)

Situações de vendas: Listar (GET /api/situacoes_vendas).

### 114. `gestaoclick_tipos_contatos_list`
**Input**: `account` (opcional), `query` (opcional)

Tipos de contatos: Listar (GET /api/tipos_contatos).

### 115. `gestaoclick_tipos_enderecos_list`
**Input**: `account` (opcional), `query` (opcional)

Tipos de endereços: Listar (GET /api/tipos_enderecos).

### 116. `gestaoclick_transportadoras_create`
**Input**: `account` (opcional), `body` (opcional)

Transportadoras: Cadastrar (POST /api/transportadoras).

### 117. `gestaoclick_transportadoras_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Transportadoras: Deletar (DELETE /api/transportadoras/{id}).

### 118. `gestaoclick_transportadoras_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Transportadoras: Visualizar (GET /api/transportadoras/{id}).

### 119. `gestaoclick_transportadoras_list`
**Input**: `account` (opcional), `query` (opcional)

Transportadoras: Listar (GET /api/transportadoras).

### 120. `gestaoclick_transportadoras_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Transportadoras: Editar (PUT /api/transportadoras/{id}).

### 121. `gestaoclick_usuarios_list`
**Input**: `account` (opcional), `query` (opcional)

Usuários: Listar (GET /api/usuarios).

### 122. `gestaoclick_vendas_create`
**Input**: `account` (opcional), `body` (opcional)

Vendas: Cadastrar (POST /api/vendas).

### 123. `gestaoclick_vendas_delete`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Vendas: Deletar (DELETE /api/vendas/{id}).

### 124. `gestaoclick_vendas_gerar_parcelas_create`
**Input**: `account` (opcional), `body` (opcional)

Vendas: Gerar parcelas (POST /api/vendas/gerar_parcelas).

### 125. `gestaoclick_vendas_get`
**Input**: `account` (opcional), `id`, `query` (opcional), `ids` (opcional)

Vendas: Visualizar (GET /api/vendas/{id}).

### 126. `gestaoclick_vendas_list`
**Input**: `account` (opcional), `query` (opcional)

Vendas: Listar (GET /api/vendas).

### 127. `gestaoclick_vendas_update`
**Input**: `account` (opcional), `id`, `body` (opcional), `ids` (opcional)

Vendas: Editar (PUT /api/vendas/{id}).

## Prompts de exemplo

```
Liste meus últimos clientes no GestãoClick
Quais vendas foram feitas este mês?
Mostre as contas a receber em aberto
```
